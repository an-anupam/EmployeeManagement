@* @{
ViewData["Title"] = "Home Page";
}

<div class="container display-flex align-items-center justify-content-center text-center">
<h3 class="display-8">Welcome To Employee Skill Management Platform</h3>
<p>&copy; Capgemini Engineering 2024</p>
</div> *@



@model List<EmployeeSkillVM>;



@* <partial name="_Notification"/> *@
<div class="container">
    <div class="row pt-4 pb-3">

        <div class="col-6">
            <h2 class="text-primary">
                Employees' List
            </h2>
        </div>
        @* <div class="col-6 text-end">
        <a asp-controller="EmployeeManage" asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Create New Employee
        </a>
        </div> *@

    </div>

        <table id="HomeTable" class="table table-bordered table-striped" cellspacing="0">
            <thead>
                <tr>
                    <th scope="col">
                        Employee Id
                    </th>
                    <th scope="col">
                        Employee Name
                    </th>
                    <th scope="col">
                        Email Id
                    </th>
                    <th scope="col">
                        Primary Skill
                    </th>
                    <th scope="col">
                        Department
                    </th>
                    <th scope="col">
                        Designation
                    </th>
                    <th scope="col"></th>
                </tr>
            </thead>
            @* <tbody>
            @foreach (var obj in Model)
            {
            <tr>
            <td>@obj.Employee.Id</td>
            <td>@obj.Employee.FirstName @obj.Employee.LastName</td>
            <td>@obj.Employee.Email</td>
            @foreach (var employeeSkill in obj.EmployeeSkills)
            {
            <td>@employeeSkill.Skill.allSkills</td>
            }
            <td>@obj.Employee.DepartmentId</td>
            <td>@obj.Employee.Designation</td>
            <td>
            <div class="w-75 btn-group" role="group">
            <a asp-controller="Home" asp-action="Views" asp-route-id="@obj.Employee.Id"
            class="btn btn-info mx-2">
            <i class="bi bi-eye"></i>View
            </a>


            </div>
            </td>
            </tr>
            }
            </tbody> *@
        </table>

</div>


@section Scripts {

    <script src="~/js/Home.js"></script>
    @{
        <partial name="_ValidationScriptsPartial.cshtml" />
    }
}

@* @section Scripts {
@{
<partial name="_ValidationScriptsPartial.cshtml" />
}
} *@