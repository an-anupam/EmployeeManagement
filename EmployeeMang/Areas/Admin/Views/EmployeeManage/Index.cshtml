@* @using System.Collections.Generic
@model List<Bulky.Models.Category> *@
@model List<Employee>
@* @using Bulky.Models *@


@* <partial name="_Notification"/> *@
<div class="container">
<div class="row pt-4 pb-3">
   
    <div class="col-6">
      <h2 class="text-primary">
        Employee List
      </h2>
    </div>
    <div class="col-6 text-end">
      <a asp-controller="EmployeeManage" asp-action="Upsert" class="btn btn-outline-info">
      <i class="bi bi-plus-circle"></i> Create New Employee
      </a>
    </div>
   
</div>

<table id="tblData" class="table table-bordered table-striped" style="width: 100%;">
   <thead >
    <tr>
      <th>
        Employee Id
      </th>
      <th>
        Employee Name
      </th>
      <th>
        Email Id
      </th>  
      <th>
        Date Of Joining
      </th>
      <th>
        Designation
      </th>
      <th>
        Department Name
      </th>
      <th></th>
    </tr>
    </thead>
     @* <tbody>
      @foreach(var obj in Model.OrderBy(order => order.Id)){
       <tr>
         <td>@obj.Id</td>
         <td>@obj.FirstName  @obj.LastName</td>
         <td>@obj.Email</td>
         <td>@obj.DOJ</td>
         <td>@obj.Designation</td>
         <td>@obj.Department.Name</td>
         <td></td>

         <td>
          <div class="w-75 btn-group" role="group">
             <a asp-controller="EmployeeManage" asp-action="Upsert" asp-route-id="@obj.Id" class="btn btn-info mx-2">
                  <i class="bi bi-pencil-square"></i>Edit            
             </a>
             <a asp-controller="EmployeeManage" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                  <i class="bi bi-trash"></i> Delete
             </a>
             </div>
         </td>
       </tr>

       }
    </tbody> *@
</table>
</div>

@section Scripts{
  <script src="~/js/employee.js"></script>
}